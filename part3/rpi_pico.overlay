/ {
    aliases {
        btn = &button0;  // Define alias so DT_ALIAS(btn) works
    };

    buttons {
        compatible = "gpio-keys";

        button0: button_0 {
            gpios = <&gpio0 20 GPIO_ACTIVE_HIGH>;
            label = "User button";
        };
    };
};
